package space.labmatt.Devices;

import java.net.Socket;

public class Relay {

    /**
     * While the socket is connected then run
     * @param socket
     * @throws Exception
     */
    public void relayFunctions(Socket socket) throws Exception {

        while (socket.isConnected()) {

        }
    }
}
